{
  "name": "survey-api",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "survey-api/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/survey-api",
        "main": "survey-api/src/main.ts",
        "tsConfig": "survey-api/tsconfig.app.json",
        "assets": ["survey-api/src/assets"],
        "webpackConfig": "survey-api/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "survey-api:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "survey-api:build:development"
        },
        "production": {
          "buildTarget": "survey-api:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "survey-api/jest.config.ts"
      }
    }
  },
  "tags": []
}
